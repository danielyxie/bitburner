/**
 * Enum defining the different types of possible locations
 */
export enum LocationType {
    CityHall,
    Company,
    Gym,
    Hospital,
    Slums,
    StockMarket,
    TechVendor,
    TravelAgency,
    University,
}
